apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  #replicas: 1
  selector:
    app: frontend
  type: "{{ .Values.service.type }}"
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
