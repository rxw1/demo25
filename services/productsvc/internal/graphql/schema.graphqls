type Product {
  id: ID!
  price: Int!
  name: String!
}

type Order {
  id: ID!
  qty: Int!
  productId: ID!
  eventId: String!
  createdAt: String!
  price: Int!
}

type Query {
  productById(productId: ID!): Product
  products: [Product!]!
  orders: [Order!]!,
  getPrice(productId: ID!): Int!
}

type Mutation {
  createOrder(productId: ID!, qty: Int!): Order
}
  