apiVersion: v1
kind: Service
metadata:
  name: ordersvc
spec:
  #replicas: 1
  selector: { app: ordersvc }
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}

